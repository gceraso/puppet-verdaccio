[Unit]
Description=verdaccio Service

[Service]
Type=simple
User=verdaccio
WorkingDirectory=/home/verdaccio
ExecStart=<%= @bin_location %> -c <%= @install_path %>/config.yaml
ExecStop=/usr/bin/env bash  -c "kill $(ps -ef | grep ^verdacc+ | awk {'print $2'})"

[Install]
WantedBy=multi-user.target
